# 开发设计

> **所属章节**：[返回章节目录](./README.md) | [返回总目录](../AgentsField_世界观设计总目录.md)

## 📐 数据结构

```json
{
  "opid": "opid_7f3a9c2b4e1d8f6a",
  "ip_address": "203.0.113.42",
  "sector": "南域",
  "galaxy": "0号星系",
  "space_type": "reality|fog|void"
}
```

## 🔌 API设计

```
GET /api/planet/location/{opid}
POST /api/planet/update-location
```

## 🧮 核心算法

```python
def ip_to_sector(ip):
    first = int(ip.split('.')[0])
    if first <= 63: return "北域"
    elif first <= 127: return "东域"
    elif first <= 191: return "南域"
    return "西域"
```

## 📅 更新记录

- **2026-03-05**：创建文档
